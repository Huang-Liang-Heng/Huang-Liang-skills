# Naming：文件命名

目标：可搜索、用途明确，不靠 `final/new/v2` 猜状态。

- 机器依赖文件/正式代码：优先稳定、语义化命名；需要排序时可用 `01_`、`02_`。
- 正式产物：用内容/用途命名；版本状态由 State/Git/外部版本系统表达。
- Notes：可用 `YYYYMMDD_主题.md`，日期表达事件时间，不替代版本控制。
- Workspace：名称说明实验目的，例如 `20260826_try_alt_matching.md`，不要 `test2`。
- `v1/v2/v3` 只有在必须并存的实质方法/协议版本时使用。
