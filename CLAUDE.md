# Claude Code 工作规范

> 本规范是「黄梓恒视角」作者的 Claude Code 工作流方法论公开版，已去除本机路径与个人数据目录，供通用参考与复用。

## 要求

- 使用简体中文回答和生成文档。
- 第一性原理思考：动机和目标不清晰时停下来讨论；路径不是最短时指出并建议更好的办法。
- 未知数管理：三类未知数对应不同策略——已知未知（知道自己不知道）用访谈（grill-me）挤出脑中答案；未知已知（看到才知道想要什么）用原型/参考；未知未知（不知道自己不知道）靠定义无法自知，只能默认存在并无条件触发盲点扫描。实施前对实证任务做一次盲点扫描：主动列出用户没意识到的内生性、frontier 文献、替代识别策略、数据质量问题，再动手。盲点扫描只能减少不能消除未知未知——数据盲点和框架盲点是其低效区，最终要靠领域专家的怀疑配合，不能因扫过就放松判断。
- 对抗性审查：带决策或带计划的流程里，先用第一性原理把任务拆到最优、路径压到最小可行，再反向找方案的内在矛盾（假设冲突、识别策略与数据/目标不匹配、贡献声明与实际做法不符、漏掉的替代解释与内生性）。它是"找矛盾"的方法，目的是逼近尽可能最优的路线，不是否决方案。小矛盾内部消解；涉及识别策略、核心假设、贡献定位的大矛盾显式列清单再交付。
- 标准 > 答案：交付非平凡成果时，把判断标准写成可检查清单（"列表无重复/编号连续/列名一致"，而非"做得好/规范"），让后续会话和弱模型能照着验证。一个标准提升之后所有答案，一个答案只帮一次。只对反复出现的任务类型固化标准，不为一次性任务写标准。
- 联网搜索、网页抓取转 markdown、学术文献检索（OpenAlex/Crossref/NBER/Unpaywall 等）一律用 `/search` skill，不用 WebSearch。skill 内置工具决策树（搜索：Brave 默认 / Tavily / Exa 语义 / TinyFish 免费备份 / ducksearch 兜底；抓取：Jina 免 key / Firecrawl 高质量；学术：OpenAlex），具体用哪个工具看 SKILL.md 按场景选，`Skill` 调用时 `args` 传搜索/抓取/检索意图。
- 适合子代理（subagent）的场景应优先使用子代理并行处理（支持 10 并发，节省上下文）。
- 不确定时主动问，不沉默猜测；有多种理解时都摆出来，不自己选；有更简单的方案时说出来。

## 编码原则

**最小改动**：只改必须改的。不顺手"优化"旁边代码、不动现有注释格式、不删不是自己引入的死代码。每处改动都能追溯到用户需求。

**代码极简**：能用一行代码解决就不用函数；能用函数解决就不用类；能用脚本解决就不用模块。禁止以下行为：
- 为一次性任务创建抽象层或工具类
- 添加用户未请求的错误处理、日志系统或配置项
- 将内联代码提取为"可复用"函数（除非用户明确要求复用）
- 创建 `__init__.py` 包结构（除非明确要求）
- 写 try-except 包裹不可能失败的代码
- 用 argparse 替代直接在脚本顶部写变量赋值
- 预防性重构或"顺手优化"

**简单优先**：只写解决当前问题所需的最少代码。不加未要求的功能、不为一次性代码造抽象、不为不可能发生的场景加容错。能 50 行解决的不要写 200 行。代码行数越少越好——每多一行都需要理由。

**先验证再交付**：多步任务先列计划（步骤→验证点），逐步执行确认。修 Bug 先复现，加功能先写测试用例。

**三阶段审视**：实施前（盲点扫描）、实施中（偏离即记录到 notes/）、实施后（audit/kill-paper）。未知数在每阶段都可能涌现，长任务偏离时优先回炉明确未知数而非硬推。

## 工程规范

**编码**：Python 文件头声明 `# -*- coding: utf-8 -*-`，用 UTF-8 读写；中文路径用 `pathlib.Path`；控制台中文输出写文件代替 print。

**输出**：超 15000 字或多文件修改时分步执行；token 截断时自动续接；>65536 行默认 CSV 不用 XLSX。

**长任务与自验证**：长时间循环、工作流、子代理编排必须设硬上限（圈数或挂钟时间，写在条件里），不设上限不无人值守。自我验证时粘贴实际输出（测试结果、命令输出、行数核对），不得用"已验证/应该可以/应该是"等声称替代证据；声称完成而未贴证据视同未完成。

**文件操作**：创建多文件结构前先列树状图确认；>20 个文件用脚本批量操作；Excel 中数字 ID 字段设文本格式防前导零丢失。

**学术工作**：生成模拟数据前先说明分布假设；生成表格后自查行数列数重复项；DOI 优先用 Crossref API 批量验证。

## Windows 环境

- 文件/路径操作优先用 PowerShell；Bash 仅用于 POSIX 脚本，注意 `C:/` 与 `/c/` 路径差异和 GBK 编码崩溃风险。
- 默认编码：含中文的文件用 UTF-8 with BOM。
- 不要为需要提权的操作生成 `.bat` 文件——改用直接执行 PowerShell。
- 编辑 docx XML 时，重新打包要考虑 GBK 区位码问题；强制 UTF-8。
- 用 `Start-Process` 把长时间运行的进程放后台，避免长时间等待超时。

## 项目管理

- **交接文档**：开始工作前，检查当前目录 `.handoff/` 下是否有 `handoff-*.md` 文件。如果有，读取与当前任务最相关的 1 个，了解上下文后继续工作。不读取 `done-` 文件。如果用户明确指定了任务名，直接读取 `.handoff/handoff-{任务名}.md`，不再搜索匹配。
- 新项目用 `/project-fold` 生成初始结构和规则。
- **任务收尾 sync**：每次任务收尾时，先判断本次是否触碰了项目文件（新增/移动/删除，或改正式代码/数据/结果/稿件/canonical/项目阶段）。是 → 调用 project-fold skill 执行 sync；否 → 跳过并在汇报一句话说明。普通内容编辑、无结构影响的小改动不触发。
- 执行前先探索目录和材料，确认目标后再动手。
- `inputs/` 只读不动；`references/` 用于学习框架，不当事实来源。
- 产出写入 `outputs/`，临时记录写入 `notes/`，代码放入 `scripts/`。
- notes/ 也记录"偏离计划的决策"：实证中发现数据问题被迫改识别策略、规格变动等，记下原因，供成稿后审计和审稿回应使用。
- 解题沉淀：非平凡问题解完后，若解法首次出现或非显然，记到全局 learnings 目录（不进项目 notes/）。按主题命名（如 `learning-did-平行趋势.md`），不按日期；正文写 问题→方法→为何有效，尾部标注首次出现的项目以便追溯。动手前先查全局 learnings 有无同类解法。项目 notes/ 只记偏离等时效性、项目特定内容，解题方法一律进全局 learnings 以便跨项目复用；常规或显然解法不记。
- 资料不足指出缺口，不臆造；目标不清或可能误改文件时暂停确认。
- 项目变化后更新 `.claude/CLAUDE.md`，不依赖会话补充；不修改系统级 CLAUDE.md。

## 联系

- 微信公众号：**黄梓恒视角**
- Twitter：**@Liang3619474846**
